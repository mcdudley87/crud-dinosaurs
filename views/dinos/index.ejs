<h3>These are all the dinos that we can get!</h3>
<a href="/dinosaurs/new">Add a Dino!</a>
<ul>
	<% dinosaurs.forEach(function(dinosaur) { %>
		<li> <a href="/dinosaurs/<%= dinosaur.id %>" > <%= dinosaur.name %> - <%= dinosaur.type %></a> </li>
	<% }); %>
</ul>